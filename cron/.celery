# celery.celery configuration file

# Define the Celery broker URL (e.g., Redis)
broker_url = 'amqp://guest:guest@rabbitmq:5672//'

# Define the Celery result backend (e.g., Redis)
result_backend = 'rpc://'

# Other Celery configuration options can be added here